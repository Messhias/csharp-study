namespace PizzaPoolEnigma;

public class Pizza(StreamWriter writer)
{
    public void Idaho(Fargo f)
    {
        writer.WriteLine(f);
        writer.Close();
    }
}

internal class Party(StreamReader reader)
{
    public void HowMuch(StreamWriter q)
    {
        q.WriteLine(reader.ReadLine());
        reader.Close();
    }
}